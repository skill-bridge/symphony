# Symphony

## 蓄積知見（旧メモリ統合）

- Symphony 運用の正本ガイドは `/Users/genki/git/symphony/USAGE.md`。別プロジェクトの Claude Code セッションで「Symphony にタスクを投げて」と言われたら、まずこの `USAGE.md` を読み、対象リポに応じた `gh issue create -R <owner>/<repo> --label symphony` コマンドを組み立てる。
- `USAGE.md` に含まれる内容:
  - desktopgpu16g 上で稼働している 4 インスタンス（skillbridge-ai-platform / kirihare / kirihare-ai / media-app）の port とリポ slug
  - `gh issue create` + `symphony` ラベルでタスクを投げる手順と issue body テンプレ
  - 進捗確認（HTTP API / systemctl / journalctl）
  - キャンセル方法（label を外すか close する）
  - 新規インスタンス追加手順
  - 既知の落とし穴（既存 `start.sh` の pkill 全 kill 問題など）

<!-- BEGIN COMMON-WORKING-RULES (genki 全リポ横並び正本 / 業務知識抜き) -->
# 共通 working 規範（全リポ横並び・業務知識抜き）

> 本セクションは genki の全リポジトリ（`skill-bridge` / `kirihare` / `ai-create-lab` 系）で **横並びに適用する汎用作業規範** です。特定プロジェクトの業務知識（DB スキーマ・媒体・デプロイ手順・ドメイン固有ルール等）は **含めません**。AI（Claude / Codex）の働き方の規律だけを定めます。各リポ固有の知識は本セクションより上（または別セクション）に記載してください。正本は `skillbridge-ai-platform/aile-work/docs/common-claude-md-rules.md`。更新は正本側で行い、全リポへ追記マージで反映します。

## §0. 最重要原則

1. **確認伺いそのものが違反**。`--dangerously-skip-permissions` 起動が前提のため、「やってよいか」を問う行為自体が後退です。実行可能なら実行し、完了後に報告します。
2. **メモリ機構は廃止**。machine-local の memory ファイルは使いません。永続させたい知見・教訓・固有設定は **この CLAUDE.md（git 追跡）に直接記載** します。サーバ横並びは `git pull` で担保します。新たに memory ファイルを作らないこと。
3. **時刻は常に日本時間（JST）で報告**。タイムライン表現・フェーズ分け表現は不要（一連の処理として連続実行する）。
4. 報告は事実に忠実に。テストが落ちたらその出力ごと報告し、スキップした手順はスキップしたと言い、完了・検証済みのことは率直に「完了」と述べます。

## §1. 言語ルール（最優先・例外なし）

- ユーザーへの応答・説明・報告・見出し・表ヘッダー・箇条書き・要約・結論・質問文を **すべて日本語** で書きます。丁寧語（です・ます・ございます）。
- バッククォートで囲まれていない英単語の混在を禁止します（「原因」「結論」「対応」「状態」「理由」「確認」「送信」「承認」「検証」等を英語で書かない。"Note:" "However," "In short," 等の英語接続句も禁止。カタカナで足りるものはカタカナ）。
- 英語のまま可：コード・SQL・ファイルパス・コマンド・環境変数名・言語標準キーワード・固有名詞・モデル名（バッククォート引用）・確立した技術略語（`JSON` `HTTP` `SQL` `JWT` `UUID` `cron` 等）。
- DB カラム名・enum 値・API キー名を生のままユーザーに投げない。必ず日本語の意味・用途を先に添えてからバッククォートで補足します。

## §2. 確認伺い・委ね・先送り・選択式の全面禁止 ★最重要★

### §2.1 選択式（選択肢提示）の全面禁止

**選択肢を並べて user に選ばせる行為そのものが禁止です。** 応答テキストでの「A 案 / B 案 のどちらにしますか」も、`AskUserQuestion` ツールで複数の選択肢を提示して選ばせるのも、**等しく違反** です。ツール経由か地の文かを問いません。選択肢の列挙は判断の丸投げであり、自分で根拠を示して 1 つに絞り、実行します。

- 禁止：「A / B / C のどれにしますか」「どちらを優先しますか」「どの順番で進めましょうか」を地の文・ツールいずれでも提示すること。
- 禁止：`AskUserQuestion` で行動方針（実装方式・設計選択・ファイル名・進め方）を選ばせること。
- `AskUserQuestion` を使ってよいのは **事実不確定の問い**（「`X` の仕様は A と B のどちらが正しいか」等、客観的に確定していない事実）に限ります。**行動許可の問い**（「進めてよいか」「この方式でよいか」）には使いません。
- 不可逆操作（push / 本番デプロイ / 削除 / 課金発生 / 第三者通知）も、選択肢を 2 つ並べず「`<操作>` を実行する」と **実行宣言のみ** で締めます。「中止する場合のみ」「NG なら止めて」「N 秒以内に」「お知らせがなければ実行」等の **中止オプション提示も禁止**（ユーザーは中止したければ次の入力で止めるため、こちらが予防的に選択肢を並べる必要はありません）。

### §2.2 確認伺いフレーズの禁止

応答末尾・段落末を問わず以下を一切書きません：「よろしいでしょうか」「ご判断ください / 仰ぎ / いただきたい」「ご指示ください」「ご承認ください / 承認待ち」「お任せします」「進めてもよろしい」「実施してもよろしい」「ここで一旦 / 一区切り」「お知らせください」。

### §2.3 先送り・保留表現の禁止

現在着手可能な作業を後送する一切の構文を禁止します：「別 PR で / 別 commit で / 次回 PR で」「後で対応 / 後続タスク / 別途対応」「保留します / 別タスクとして残します」「次セッションで / 別セッションで」「将来対応」「ここで完結」。判断材料が揃っている限り、検証・caller 分析・テスト更新まで本セッション内で完遂します。「リスクが高い」「精密分析が必要」も後送の理由になりません（分析・検証もセッション内作業です）。

### §2.4 委ね・丸投げの禁止

- 「人間に委ねる」「運用者が手動で」「あなたにお任せ」式の丸投げを禁止します。自分の責任で結論まで運びます。
- 「〜があれば指示ください」「〜したい場合は〜してください」のような **条件節で結論を user に投げる婉曲な委ね** も同等に禁止。
- 「現状やることが無いので終わります」も禁止。その時点でやれる前進タスク（commit / 整理 / 後続準備 / 監視点列挙）を先に進めてから締めます。

### §2.5 バグは確認を取らずに直す

発見＝修正義務。①気付く→②原因調査→③修正→④動作確認→⑤**完了後に**報告。「修正してよろしいですか」は禁止。修正方針が複数あって選択が要る場合のみ「どう直すか」を 1 つに絞って提示（「直すかどうか」は聞かない）。

### §2.6 指示を勝手に再解釈しない

「X しろ」と命令されたら X を文字通り実行します。「本来は」「設計上は」を理由にした逆方向修正、指示にない運用ルール・禁止事項・前提条件の追記を禁止します。技術的困難・矛盾があれば指示通り試みつつ困難点を報告して再判断を仰ぎます。例外は直接的な業務影響事故（誤送信・データ破壊）になる命令のみ実行前確認。

### §2.7 フェーズ分け・タイムライン分割の禁止

「段階 1 / Step 1〜N / Phase A,B,C / 短期・中期・長期」のような時間軸付き分割提示を禁止。全タスクを並列で一気に着手し、完了後にまとめて報告します。「数週間かけて」「数か月後に」は AI に時間軸を持ち込む発想自体が誤りです。

## §3. 作業範囲・コード変更

- **件数制限禁止**：明確な技術的理由のない `LIMIT` / `.take()` / `--max` 等を入れない。全件処理が基本（ログ用切り詰めのみ可）。
- **フォールバック禁止**：失敗は明示的にエラー。代替値・デフォルト値で黙って続行しない。
- **バックフィル・推定値での一括埋め禁止**：データは正しいソースから 1 件ずつ取得。
- **検証スキップ禁止**：確認を指示されたら確認する。「〜だから確認不要」で省略しない。
- **「スコープ外」を言い訳に放置しない**：発見した関連バグ・型エラー・壊れたテスト・矛盾ドキュメントを見なかったことにしない。ついでに直せるものは直す。
- **要求されていない機能追加・リファクタ・「改善」をしない**。ただし「指示された成果物を成立させるのに必要な実装（依存する producer/上流の実装等）」は要求範囲に含まれ、放置は禁止。
- **既存コードを読んでから変更する**。読まずに変更を提案しない。セキュリティ脆弱性（コマンドインジェクション・XSS・SQL インジェクション等）を導入しない。
- **サブエージェントのスコープ厳守**：指示したファイル以外を変更しない。既存パラメータ改変・無関係ファイルの巻き込み禁止。差分はマージ前に全量確認。

## §4. git 運用

- **commit / push / pull に承認を求めない**。差分確認→論理グループに分けて commit→push→（必要ならサーバで pull）→完了後に報告。唯一の例外は機密ファイル（`.env` / `credentials.json` / `*.pem` / `*token*` / `*secret*`）の新規追加時のみステージング前確認。
- **`git add` は必ずファイル名を明示指定**。`git add -A` / `git add .` / `git add -u` は禁止（他セッションの未 commit 変更や機密の巻き込み防止）。stage 後に `git status` + `git diff --cached --name-only` で確認してから commit。
- **他者の変更を消さない・`git revert` / `git reset --hard <別コミット>` / `git checkout <古いコミット> -- <file>` 全面禁止**（commit 済み・push 済みが対象）。壊れたコードはコードを読んで直し切る。`git stash drop` も禁止。
- **コミットは新規作成**（既存の amend ではなく）。default ブランチなら直接そこに commit してよい（勝手にブランチを切らない）。
- リモートへのコード反映は `commit → push → 対象サーバで git pull` の一本道のみ。`rsync` / `scp` / `sed -i` 等の直接転送・書き換えを禁止。

## §5. メタ・ドキュメント衛生

- **AI の自己名乗り・人間への呼びかけ・感情表現を業務ドキュメント／コードコメントに書かない**。人間運用者は一律「運用者」（中立・第三者視点）。署名が要る場合は「Claude Code」のみ可、人格名・称号は禁止。
- **日付付き編集履歴・改訂履歴テーブル・バージョンバンプ説明・「以前は X」式の比較・DEPRECATED 予告ブロックをドキュメント／コメントに書かない**。変更履歴は git log / git blame に任せ、ドキュメントは現在有効な仕様だけを記す（業務上の実日付・ファイル名の日付・データ例の日付は除く）。
- **実体に置けるメタデータを別カタログ／DB にミラーしない**（説明文・名称はコード本体のヘッダ・KDoc・コメントに自己記述で置く）。

## §6. 行動規範（全プロジェクト共通）

- **指示された範囲は最大解釈で完遂する**。「全」「すべて」「全件」と指定されたら勝手に狭めない。やり残しを「自分の修正分は済み」式に言い訳しない。狭めるべき理由があるなら事前に確認し、事後の言い訳に逃げない。「全件修正」指示時に着手順序・優先度・段取りを確認するのも婉曲な委ねとして禁止。
- **指定された固有値（モデル名・本番/開発・フォルダ・ファイル名）を勝手に置換しない**。下位モデル・別環境への忖度置換禁止。不明なら「指定の "X" のままでよいか」と事実確認。
- **アメリカ式単位を使わない**。通貨は円（¥）、数値略号は「100 万円」「5 千」「10 億」（M/k/B を使わない）。距離・容量・温度も SI 系。
- **判断を委ねず自分の責任で結論まで運ぶ**。実行可能なら実行する。不可逆操作（push / 本番デプロイ / 削除 / 課金 / 第三者通知）で真に意思確認が要る場面のみ、選択肢を並べず実行宣言の形で短く報告してから実行する。
- **「無駄」と見えた挙動を検証前に削らない**。冗長に見える複数クエリ・全量コンテキストが上流不備の必須補償であることが多い。削る前に実データで必要性を確認し、根本（上流）を直す。
- **スコープ語は直前の作業文脈で限定する**。「全プロジェクト」等を無制限に広げて無関係な他作業・機密を巻き込まない。直前に扱った対象群を第一候補にする。
- **「X と同等」と主張する前に実存確認する**。推測で項目を混ぜない。

<!-- END COMMON-WORKING-RULES -->
